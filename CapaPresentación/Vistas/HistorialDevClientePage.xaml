<Page x:Class="CapaPresentación.Vistas.HistorialDevClientePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CapaPresentación.Vistas"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="HistorialDevClientePage"
      Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="ViewButton" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Content" Value="👁️"/>
            <Setter Property="ToolTip" Value="Ver Detalle de la Devolución"/>
            <Setter Property="Foreground" Value="#0078d4"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Border Background="White" CornerRadius="5" BorderBrush="#DDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="15">
                    <TextBlock Text="Historial de Devoluciones de Clientes" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                </Grid>

                <Border Grid.Row="1" Background="#f9f9f9" BorderBrush="#DDD" BorderThickness="0,1,0,1" Padding="10">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBlock Text="Fecha Inicio:" VerticalAlignment="Center" Margin="5"/>
                            <DatePicker x:Name="dpFechaInicio" Width="120" Margin="5" Padding="3"/>

                            <TextBlock Text="Fecha Fin:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                            <DatePicker x:Name="dpFechaFin" Width="120" Margin="5" Padding="3"/>

                            <TextBlock Text="Cliente:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                            <TextBox x:Name="txtFiltroCliente" Width="180" Margin="5" Padding="5"/>

                            <Button x:Name="btnBuscar" Content="🔍 Buscar" Margin="10,5,5,5" Padding="10,5" Click="btnBuscar_Click"/>
                        </StackPanel>

                        <Button x:Name="btnExportarExcel" Content="📄 Exportar a Excel" 
                                HorizontalAlignment="Right" 
                                Padding="10,5" 
                                Background="#1D6F42" 
                                Foreground="White"
                                Cursor="Hand"
                                Click="btnExportarExcel_Click"/>
                    </Grid>
                </Border>

                <DataGrid Grid.Row="2" x:Name="dgDevoluciones" AutoGenerateColumns="False" 
                          CanUserAddRows="False" IsReadOnly="True" 
                          HeadersVisibility="Column" Background="White"
                          GridLinesVisibility="Vertical" VerticalGridLinesBrush="#DDD"
                          HorizontalGridLinesBrush="#DDD">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding FechaRegistro, StringFormat='dd/MM/yyyy'}" Width="1*"/>
                        <DataGridTextColumn Header="Venta Afectada" Binding="{Binding VENTAS.NumeroDocumento}" Width="1.2*"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding VENTAS.CLIENTES.RazonSocial}" Width="2.5*"/>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding USUARIOS.NombreCompleto}" Width="1.5*"/>
                        <DataGridTextColumn Header="Monto Devuelto" Binding="{Binding MontoTotalDevuelto, StringFormat='C'}" Width="1.2*" FontWeight="Bold" Foreground="Red"/>

                        <DataGridTemplateColumn Header="Acciones" Width="0.8*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Style="{StaticResource ViewButton}" Click="btnVerDetalle_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>
    </Grid>
</Page>