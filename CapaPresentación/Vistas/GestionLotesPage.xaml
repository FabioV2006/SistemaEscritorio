<Page x:Class="CapaPresentación.Vistas.GestionLotesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CapaPresentación.Vistas"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="GestionLotesPage"
      Loaded="Page_Loaded">

    <Page.Resources>
        <Style x:Key="AccionButtonBase" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="EditButton" TargetType="Button" BasedOn="{StaticResource AccionButtonBase}">
            <Setter Property="Content" Value="✏️"/>
            <Setter Property="ToolTip" Value="Editar Lote"/>
            <Setter Property="Foreground" Value="Blue"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Border Background="White" CornerRadius="5" BorderBrush="#DDD" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="15">
                    <TextBlock Text="Gestión de Inventario (Lotes)" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                </Grid>

                <Border Grid.Row="1" Background="#f9f9f9" BorderBrush="#DDD" BorderThickness="0,1,0,1" Padding="10">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBlock Text="Filtrar por Producto:" VerticalAlignment="Center" Margin="5"/>
                            <TextBox x:Name="txtFiltroProducto" Width="150" Margin="5" Padding="5"/>

                            <TextBlock Text="Filtrar por Lote:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                            <TextBox x:Name="txtFiltroLote" Width="120" Margin="5" Padding="5"/>

                            <TextBlock Text="Vencen antes de:" VerticalAlignment="Center" Margin="15,5,5,5"/>
                            <DatePicker x:Name="dpFiltroVencimiento" Width="120" Margin="5" Padding="3"/>

                            <Button x:Name="btnBuscar" Content="🔍 Buscar" Margin="10,5,5,5" Padding="10,5" Click="btnBuscar_Click"/>
                        </StackPanel>

                        <Button x:Name="btnExportarExcel" Content="📄 Exportar a Excel" 
                                HorizontalAlignment="Right" 
                                Padding="10,5" 
                                Background="#1D6F42" 
                                Foreground="White"
                                Cursor="Hand"
                                Click="btnExportarExcel_Click"/>
                    </Grid>
                </Border>

                <DataGrid Grid.Row="2" x:Name="dgLotes" AutoGenerateColumns="False" 
                          CanUserAddRows="False" IsReadOnly="True" 
                          HeadersVisibility="Column"
                          Background="White"
                          GridLinesVisibility="Vertical" VerticalGridLinesBrush="#DDD"
                          HorizontalGridLinesBrush="#DDD">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Producto" Binding="{Binding PRODUCTOS.Nombre}" Width="2*"/>
                        <DataGridTextColumn Header="Categoría" Binding="{Binding PRODUCTOS.CATEGORIAS.Descripcion, FallbackValue='N/A'}" Width="1.2*"/>
                        <DataGridTextColumn Header="Laboratorio" Binding="{Binding PRODUCTOS.LABORATORIOS.Nombre, FallbackValue='N/A'}" Width="1.5*"/>
                        <DataGridTextColumn Header="Nro. Lote" Binding="{Binding NumeroLote}" Width="1*"/>
                        <DataGridTextColumn Header="Vencimiento" Binding="{Binding FechaVencimiento, StringFormat='dd/MM/yyyy'}" Width="1*"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="0.6*" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Precio Compra" Binding="{Binding PrecioCompra, StringFormat='C'}" Width="1*"/>
                        <DataGridTextColumn Header="Precio Venta" Binding="{Binding PrecioVenta, StringFormat='C'}" Width="1*"/>
                        <DataGridTextColumn Header="Ubicación" Binding="{Binding UbicacionAlmacen}" Width="1*"/>

                        <DataGridTemplateColumn Header="Acciones" Width="0.8*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Style="{StaticResource EditButton}" Click="btnEditar_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
        </Border>
    </Grid>
</Page>